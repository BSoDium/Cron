sdk=34
# Use a stock Application — the real CronApplication.onCreate() enqueues WorkManager jobs and
# creates notification channels, which aren't wanted (and aren't initialized) under unit tests.
application=android.app.Application
